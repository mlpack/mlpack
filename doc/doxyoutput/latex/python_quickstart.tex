\doxysection{Introduction}\label{python_quickstart_python_quickstart_intro}
This page describes how you can quickly get started using mlpack from Python and gives a few examples of usage, and pointers to deeper documentation.

This quickstart guide is also available for \doxyref{the command-\/line}{p.}{cli_quickstart} and \doxyref{Julia}{p.}{julia_quickstart}.\doxysection{Installing mlpack}\label{python_quickstart_python_quickstart_install}
Installing the mlpack bindings for Python is straightforward. It\textquotesingle{}s easy to use conda or pip to do this\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{pip install mlpack}

\end{DoxyCode}



\begin{DoxyCode}{0}
\DoxyCodeLine{conda install -\/c conda-\/forge mlpack}

\end{DoxyCode}


Otherwise, we can build the Python bindings from scratch, as follows. First we have to install the dependencies (the code below is for Ubuntu), then we can build and install mlpack. You can copy-\/paste the commands into your shell.


\begin{DoxyCode}{0}
\DoxyCodeLine{sudo apt-\/get install libboost-\/all-\/dev g++ cmake libarmadillo-\/dev python-\/pip wget}
\DoxyCodeLine{sudo pip install cython setuptools distutils numpy pandas}
\DoxyCodeLine{wget https://www.mlpack.org/files/mlpack-\/3.4.2.tar.gz}
\DoxyCodeLine{tar -\/xvzpf mlpack-\/3.4.2.tar.gz}
\DoxyCodeLine{mkdir -\/p mlpack-\/3.4.2/build/ \&\& cd mlpack-\/3.4.2/build/}
\DoxyCodeLine{cmake ../ \&\& make -\/j4 \&\& sudo make install}

\end{DoxyCode}


More information on the build process and details can be found on the \doxyref{Building mlpack From Source}{p.}{build} page. You may also need to set the environment variable {\ttfamily LD\+\_\+\+LIBRARY\+\_\+\+PATH} to include {\ttfamily /usr/local/lib/} on most Linux systems.


\begin{DoxyCode}{0}
\DoxyCodeLine{export LD\_LIBRARY\_PATH=/usr/local/lib/}

\end{DoxyCode}


You can also use the mlpack Docker image on Dockerhub, which has all of the Python bindings pre-\/installed\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{docker run -\/it mlpack/mlpack /bin/bash}

\end{DoxyCode}
\doxysection{Simple mlpack quickstart example}\label{python_quickstart_python_quickstart_example}
As a really simple example of how to use mlpack from Python, let\textquotesingle{}s do some simple classification on a subset of the standard machine learning {\ttfamily covertype} dataset. We\textquotesingle{}ll first split the dataset into a training set and a testing set, then we\textquotesingle{}ll train an mlpack random forest on the training data, and finally we\textquotesingle{}ll print the accuracy of the random forest on the test dataset.

You can copy-\/paste this code directly into Python to run it.


\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{import} mlpack}
\DoxyCodeLine{\textcolor{keyword}{import} pandas \textcolor{keyword}{as} pd}
\DoxyCodeLine{\textcolor{keyword}{import} numpy \textcolor{keyword}{as} np}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{\# Load the dataset from an online URL.  Replace with 'covertype.csv.gz' if you}}
\DoxyCodeLine{\textcolor{comment}{\# want to use on the full dataset.}}
\DoxyCodeLine{df = pd.read\_csv(\textcolor{stringliteral}{'http://www.mlpack.org/datasets/covertype-\/small.csv.gz'})}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{\# Split the labels.}}
\DoxyCodeLine{labels = df[\textcolor{stringliteral}{'label'}]}
\DoxyCodeLine{dataset = df.drop(\textcolor{stringliteral}{'label'}, 1)}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{\# Split the dataset using mlpack.  The output comes back as a dictionary,}}
\DoxyCodeLine{\textcolor{comment}{\# which we'll unpack for clarity of code.}}
\DoxyCodeLine{output = mlpack.preprocess\_split(input=dataset,}
\DoxyCodeLine{                                 input\_labels=labels,}
\DoxyCodeLine{                                 test\_ratio=0.3)}
\DoxyCodeLine{training\_set = output[\textcolor{stringliteral}{'training'}]}
\DoxyCodeLine{training\_labels = output[\textcolor{stringliteral}{'training\_labels'}]}
\DoxyCodeLine{test\_set = output[\textcolor{stringliteral}{'test'}]}
\DoxyCodeLine{test\_labels = output[\textcolor{stringliteral}{'test\_labels'}]}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{\# Train a random forest.}}
\DoxyCodeLine{output = mlpack.random\_forest(training=training\_set,}
\DoxyCodeLine{                              labels=training\_labels,}
\DoxyCodeLine{                              print\_training\_accuracy=\textcolor{keyword}{True},}
\DoxyCodeLine{                              num\_trees=10,}
\DoxyCodeLine{                              minimum\_leaf\_size=3)}
\DoxyCodeLine{random\_forest = output[\textcolor{stringliteral}{'output\_model'}]}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{\# Predict the labels of the test points.}}
\DoxyCodeLine{output = mlpack.random\_forest(input\_model=random\_forest,}
\DoxyCodeLine{                              test=test\_set)}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{\# Now print the accuracy.  The 'probabilities' output could also be used}}
\DoxyCodeLine{\textcolor{comment}{\# to generate an ROC curve.}}
\DoxyCodeLine{correct = np.sum(}
\DoxyCodeLine{    output[\textcolor{stringliteral}{'predictions'}] == np.reshape(test\_labels, (test\_labels.shape[0],)))}
\DoxyCodeLine{print(str(correct) + \textcolor{stringliteral}{' correct out of '} + str(len(test\_labels)) + \textcolor{stringliteral}{' ('} +}
\DoxyCodeLine{    str(100 * float(correct) / float(len(test\_labels))) + \textcolor{stringliteral}{'\%).'})}

\end{DoxyCode}


We can see that we achieve reasonably good accuracy on the test dataset (80\%+); if we use the full {\ttfamily covertype.\+csv.\+gz}, the accuracy should increase significantly (but training will take longer).

It\textquotesingle{}s easy to modify the code above to do more complex things, or to use different mlpack learners, or to interface with other machine learning toolkits.\doxysection{What else does mlpack implement?}\label{python_quickstart_python_quickstart_whatelse}
The example above has only shown a little bit of the functionality of mlpack. Lots of other commands are available with different functionality. A full list of each of these commands and full documentation can be found on the following page\+:


\begin{DoxyItemize}
\item {\texttt{ Python documentation}}
\end{DoxyItemize}

For more information on what mlpack does, see {\texttt{ https\+://www.\+mlpack.\+org/}}. Next, let\textquotesingle{}s go through another example for providing movie recommendations with mlpack.\doxysection{Using mlpack for movie recommendations}\label{python_quickstart_python_quickstart_movierecs}
In this example, we\textquotesingle{}ll train a collaborative filtering model using mlpack\textquotesingle{}s {\ttfamily {\texttt{ cf()}}} method. We\textquotesingle{}ll train this on the Movie\+Lens dataset from {\texttt{ https\+://grouplens.\+org/datasets/movielens/}}, and then we\textquotesingle{}ll use the model that we train to give recommendations.

You can copy-\/paste this code directly into Python to run it.


\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{import} mlpack}
\DoxyCodeLine{\textcolor{keyword}{import} pandas \textcolor{keyword}{as} pd}
\DoxyCodeLine{\textcolor{keyword}{import} numpy \textcolor{keyword}{as} np}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{\# First, load the MovieLens dataset.  This is taken from files.grouplens.org/}}
\DoxyCodeLine{\textcolor{comment}{\# but reposted on mlpack.org as unpacked and slightly preprocessed data.}}
\DoxyCodeLine{ratings = pd.read\_csv(\textcolor{stringliteral}{'http://www.mlpack.org/datasets/ml-\/20m/ratings-\/only.csv.gz'})}
\DoxyCodeLine{movies = pd.read\_csv(\textcolor{stringliteral}{'http://www.mlpack.org/datasets/ml-\/20m/movies.csv.gz'})}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{\# Hold out 10\% of the dataset into a test set so we can evaluate performance.}}
\DoxyCodeLine{output = mlpack.preprocess\_split(input=ratings, test\_ratio=0.1, verbose=\textcolor{keyword}{True})}
\DoxyCodeLine{ratings\_train = output[\textcolor{stringliteral}{'training'}]}
\DoxyCodeLine{ratings\_test = output[\textcolor{stringliteral}{'test'}]}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{\# Train the model.  Change the rank to increase/decrease the complexity of the}}
\DoxyCodeLine{\textcolor{comment}{\# model.}}
\DoxyCodeLine{output = mlpack.cf(training=ratings\_train,}
\DoxyCodeLine{                   test=ratings\_test,}
\DoxyCodeLine{                   rank=10,}
\DoxyCodeLine{                   verbose=\textcolor{keyword}{True},}
\DoxyCodeLine{                   algorithm=\textcolor{stringliteral}{'RegSVD'})}
\DoxyCodeLine{cf\_model = output[\textcolor{stringliteral}{'output\_model'}]}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{\# Now query the 5 top movies for user 1.}}
\DoxyCodeLine{output = mlpack.cf(input\_model=cf\_model,}
\DoxyCodeLine{                   query=[[1]],}
\DoxyCodeLine{                   recommendations=10,}
\DoxyCodeLine{                   verbose=\textcolor{keyword}{True})}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{\# Get the names of the movies for user 1.}}
\DoxyCodeLine{print(\textcolor{stringliteral}{"{}Recommendations for user 1:"{}})}
\DoxyCodeLine{\textcolor{keywordflow}{for} i \textcolor{keywordflow}{in} range(10):}
\DoxyCodeLine{  print(\textcolor{stringliteral}{"{}  "{}} + str(i) + \textcolor{stringliteral}{"{}: "{}} + str(movies.loc[movies[\textcolor{stringliteral}{'movieId'}] ==}
\DoxyCodeLine{      output[\textcolor{stringliteral}{'output'}][0, i]].iloc[0][\textcolor{stringliteral}{'title'}]))}

\end{DoxyCode}


Here is some example output, showing that user 1 seems to have good taste in movies\+:


\begin{DoxyCode}{0}
\DoxyCodeLine{Recommendations for user 1:}
\DoxyCodeLine{  0: Casablanca (1942)}
\DoxyCodeLine{  1: Pan's Labyrinth (Laberinto del fauno, El) (2006)}
\DoxyCodeLine{  2: Godfather, The (1972)}
\DoxyCodeLine{  3: Answer This! (2010)}
\DoxyCodeLine{  4: Life Is Beautiful (La Vita è bella) (1997)}
\DoxyCodeLine{  5: Adventures of Tintin, The (2011)}
\DoxyCodeLine{  6: Dark Knight, The (2008)}
\DoxyCodeLine{  7: Out for Justice (1991)}
\DoxyCodeLine{  8: Dr. Strangelove or: How I Learned to Stop Worrying and Love the Bomb (1964)}
\DoxyCodeLine{  9: Schindler's List (1993)}

\end{DoxyCode}
\doxysection{Next steps with mlpack}\label{python_quickstart_python_quickstart_nextsteps}
Now that you have done some simple work with mlpack, you have seen how it can easily plug into a data science workflow in Python. A great thing to do next would be to look at more documentation for the Python mlpack bindings\+:


\begin{DoxyItemize}
\item {\texttt{ Python mlpack binding documentation}}
\end{DoxyItemize}

Also, mlpack is much more flexible from C++ and allows much greater functionality. So, more complicated tasks are possible if you are willing to write C++ (or perhaps Cython). To get started learning about mlpack in C++, the following resources might be helpful\+:


\begin{DoxyItemize}
\item {\texttt{ mlpack C++ tutorials}}
\item {\texttt{ mlpack build and installation guide}}
\item {\texttt{ Simple sample C++ mlpack programs}}
\item {\texttt{ mlpack Doxygen documentation homepage}} 
\end{DoxyItemize}