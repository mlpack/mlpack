package org.mlpack;

import org.bytedeco.javacpp.*;

public class ${model_name}Type {
  private final Pointer pointer;

  ${model_name}Type(Pointer pointer) {
    this.pointer = pointer;
  }

  Pointer getPointer() {
    return pointer;
  }
}