package org.mlpack;

import org.bytedeco.javacpp.*;

public class ${model_name} {
  private final Pointer pointer;

  ${model_name}(Pointer pointer) {
    this.pointer = pointer;
  }

  Pointer getPointer() {
    return pointer;
  }
}